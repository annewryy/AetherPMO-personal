# 동료 커밋 분석 리포트 — 2026-07-06

- **분석 범위**: `4e6158c1`..`7128b5ae` (origin/main)
- **커밋 수**: 33건, 전부 annervyy (동료)
- **변경 파일**: `app.js`(+1987/-), `index.html`(+289/-), `style.css`(+541/-), `api/g2b.js`(+372/-), `projects_import.csv`(신규, 14 rows)
- **pms_*.sql / web/src/lib/dataClient.ts 변경 없음** — 이번 배치는 전부 루트의 기존 바닐라 JS 앱(레거시 `projects`/`project_members` 테이블) 영역에서 발생.

## 1. 나라장터(G2B) 실시간 공고조회 연동
커밋: `05999eb`,`7e56eb3`,`21fd7e2`,`b158d6d`,`60de35b`,`bdc2a4a`,`95fd1c8`,`941cb34`,`eab3801`,`ad24c0a`(일부),`f590a84`,`f59ad4b`,`1bc8694`,`ec09c0b`,`49539bf`,`9e10dd8`,`e89356e`,`cee178a`,`d250a2a`,`6bb5446`,`82b8b7c`,`161f2e4`,`a6dad1f`,`fc65575`,`51bd3bcad`,`3f91231`,`f32d567`,`4aaa1ed`,`30c12cd`,`cd6a77a`,`13a05af`,`7128b5a`

① **기능 요약**: 나라장터 OpenAPI(조달청)를 `api/g2b.js`(Vercel 서버리스 프록시)를 통해 연동, 입찰단계 화면 우측에 공고 목록을 실시간 검색·조회하는 패널 구현. API 호출 한도(6개월/186일 제한, rate limit)를 보완하기 위해 하이브리드 검색(로컬 캐시 + 원격 조회)과 90일 자동 확장 조회 모델을 도입. 검색 버튼/엔터키 즉시검색, 한 글자 검색 차단 가드, 대소문자 무시 부분검색 등 UX를 다듬음. 조회된 공고(`announcementNo`, `bidNtceNm`, `dminsttNm` 등)에서 "입찰 등록" 클릭 시 레거시 `projects` 테이블에 신규 행을 생성.

② **영향 영역**: [UI 전용] — `app.js`/`index.html`/`style.css`의 화면 로직, `api/g2b.js`는 기존에 이미 존재하던 프록시 함수의 확장(신규 엔드포인트 아님, `vercel.json` 변경 없음).

③ **판단 제안**: **무시 가능**. 나라장터 연동은 레거시 바닐라 JS 앱의 자체 기능이며, `pms_*` 스키마나 `web/` Vue 프론트와는 별개 경로로 동작한다. 다만 참고로: `pms_supabase_schema.sql`에는 이미 `announcement_no`, `proposal_deadline` 컬럼이 있고 [0001-bid-to-exec-lineage.md](../0001-bid-to-exec-lineage.md)의 스폰 스냅샷 목록에도 `announcement_no`가 포함되어 있어, 향후 pms_* 쪽에 실제 G2B 연동을 구현할 때 이번 레거시 구현(필드명 매핑, 6개월 제한 대응 패턴)을 참고 자료로 재사용할 수 있다.

## 2. `project_code` / `bid_number` 역할 분리
커밋: `51bd3bcad`(project_code와 bid_number 컬럼 역할 분리), `fc65575`(bid_number 컬럼 누락 오류 수정), `3f91231`(bid_status 문자열 공백 일치화)

① **기능 요약**: 레거시 `projects` 테이블에서 `project_code`(내부 코드)와 `bid_number`(나라장터 공고번호)를 명확히 분리 저장하도록 수정. 이전에는 공고번호를 `project_code`에 그대로 넣어 UNIQUE 충돌이 발생하던 버그를 고침.

② **영향 영역**: [UI 전용/레거시 스키마 사용] — `schema.sql`(루트 레거시)의 `projects` 테이블은 이미 `project_code`, `bid_number`, `bid_status` 컬럼을 보유하고 있어 DDL 변경 없이 기존 컬럼을 올바르게 채워 넣는 로직 수정에 해당.

③ **판단 제안**: **무시 가능**. `pms_project`(신규 스키마)는 이미 `bid_number`/`announcement_no`를 별도 컬럼으로 두고 있어 동일한 역할 분리가 설계에 선반영되어 있다. 충돌 없음.

## 3. 인증/세션 지속성 변경
커밋: `6bb5446`(Supabase Client sessionStorage로 전환 — 세션 전용 지속성), `d250a2a`(app-section/ai-chat-widget 기본 숨김으로 레이아웃 플래시 방지)

① **기능 요약**: Supabase Auth 세션을 `localStorage` 대신 `sessionStorage`에 저장하도록 변경(브라우저 탭 종료 시 로그아웃되는 정책으로 전환). 로그인 판정 전 UI가 잠깐 보이는 플래시 현상을 막기 위해 HTML에서 기본 `display:none` 처리.

② **영향 영역**: [UI 전용] — 레거시 앱의 Supabase JS 클라이언트 옵션 변경. `web/` Vue 프론트에는 아직 인증 코드가 없어 직접 충돌 없음.

③ **판단 제안**: **설계 반영 권장(참고용, 낮은 우선순위)**. 세션 지속성 정책(탭 종료 시 로그아웃)은 보안 관점의 의도적 결정으로 보이며, `web/` Vue 프론트에서 인증을 구현할 때 동일 정책(sessionStorage vs localStorage)을 따를지 여부를 설계 문서에 명시해두면 두 프론트 간 로그인 지속 시간이 달라 사용자가 혼란을 겪는 것을 예방할 수 있다. `[[amaranth-integration-boundary]]` 메모에 정리된 "신원/로그인 이원화" 원칙과 함께 검토할 가치가 있음(단, 이 메모는 아마란스-외부인력 경계에 관한 것이라 직접 충돌은 아님).

## 4. 상태 동기화 버그 수정 (Supabase ↔ 로컬 state)
커밋: `cee178a`(loadStateFromSupabase의 const 재할당 TypeError 수정), `e89356e`(read 에러 및 null 카드 상태 매핑 방어), `9e10dd8`(디버그 로깅, status 값 trim), `49539bf`(projects 뷰 진입 시 loadStateFromSupabase 호출), `ec09c0b`(seed status를 'In Progress'로 정렬), `1bc8694`(fallback seed 12건 자동 추가), `f59ad4b`(CSV 대량 임포트: 인코딩 자동감지/중복 해소/트랜잭션), `f590a84`(saveProjectForm 비동기 트랜잭션 저장 및 에러 캐치)

① **기능 요약**: DB 동기화 안정성 버그 수정 다수(런타임 에러 수정, null-safety, seed 데이터 정합성) + CSV 프로젝트 대량 임포트 기능 신규 추가(`projects_import.csv` 샘플 데이터 포함).

② **영향 영역**: [UI 전용] — 전부 레거시 `app.js`의 클라이언트 로직/버그 수정. DB 스키마나 dataClient 계약과 무관.

③ **판단 제안**: **무시 가능**. 버그 수정 및 UI 기능으로 설계 영향 없음.

## 5. 참여인력 관리(Resource Management) 신규 화면
커밋: `ad24c0a`

① **기능 요약**: 회의록 하위에 참여인력 관리 뷰 신설(엑셀 스타일 인라인 편집, 프로젝트 필터, CSV 내보내기).

② **영향 영역**: [UI 전용] — `project_members` 테이블의 기존 컬럼(`department`, `position`, `participation_role`, `is_project_manager`)만 사용, `project_members_ddl.sql`과 일치.

③ **판단 제안**: **무시 가능**. 스키마 변경 없이 기존 컬럼을 노출하는 화면.

## 6. 기타 UI/UX 폴리시
커밋: `161f2e4`(대시보드 UI 보완), `82b8b7c`(발표용 정보 우선순위 재설계 v2), `808417a`(멤버 모달 높이/flex 레이아웃 수정), `f32d567`(입찰단계/공고조회 테이블 UI 개선)

① **기능 요약**: 순수 시각 디자인/레이아웃 조정.

② **영향 영역**: [UI 전용]

③ **판단 제안**: **무시 가능**.

---

## 종합 권고
이번 배치(33건)는 전부 루트 레거시 바닐라 JS 앱 영역에서 발생했고 `pms_*` 스키마·`dataClient.ts` 계약·CONFIRMED 설계 문서와 직접 충돌하는 내용은 없다. 유일하게 참고할 가치가 있는 것은 (1) 나라장터 G2B 연동의 실제 필드 매핑/6개월 제한 대응 패턴 — 향후 `pms_project`에 동일 연동을 구현할 때 참조 가능, (2) 세션 지속성 정책(sessionStorage) — `web/` Vue 프론트 인증 설계 시 정책 일관성 여부를 결정해두는 것을 권장. 둘 다 시급하지 않으므로 별도 설계 문서 신설 없이 다음 인증 관련 설계 착수 시 참고 메모 정도로 남겨두면 충분하다.
